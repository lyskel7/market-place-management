export enum Env {
  prod = 'prod',
  stg = 'stg',
  dev = 'dev',
}

export enum Versions {
  v1 = 'v1',
}

export enum ETypes {
  CATEGORY = 'category',
  SUBCATEGORY = 'subcategory',
  POST = 'post',
  PROFILE = 'profile',
}
